{
  "description": "A GitHub Action for turning working directory changes into pull request review suggestions",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --platform=node --format=esm --outdir=dist index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@octokit/action": "^6.1.0",
    "parse-git-diff": "^0.0.15"
  },
  "devDependencies": {
    "@octokit/webhooks-types": "^7.5.0",
    "esbuild": "^0.20.2"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "release": {
    "branches": [
      "main"
    ]
  },
  "version": "1.0.2"
}
